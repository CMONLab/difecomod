% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compositions.R
\name{core_extraction}
\alias{core_extraction}
\title{Extract Core Microbiota from Abundance Data}
\usage{
core_extraction(
  abundance_matrix,
  prevalence_threshold = 1,
  min_prevalence = 0.1
)
}
\arguments{
\item{abundance_matrix}{Numeric matrix of relative abundances (taxa x samples)}

\item{prevalence_threshold}{Numeric threshold (abundance percentage) for determining taxa prevalence (default: 0.1%)}

\item{min_prevalence}{Minimum prevalence (fraction of samples) required to retain taxa (default: 0.1)}
}
\value{
Numeric matrix of relative abundances (taxa x samples) with filtered taxa
}
\description{
Filters taxa from a relative abundance matrix based on prevalence across samples
}
\examples{
# Create example count abundance data
set.seed(456)
abundance_matrix <- matrix(runif(40, 0, 0.1), nrow = 5, ncol = 8)
colnames(abundance_matrix) <- paste0("Sample_", 1:8)
rownames(abundance_matrix) <- paste0("Taxa_", 1:5)

# Convert to occurrence matrix
core_abundance_matrix <- core_extraction(abundance_matrix, prevalence_threshold = 5, min_prevalence = 0.5)

}
