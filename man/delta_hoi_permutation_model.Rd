% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hoi.R
\name{delta_hoi_permutation_model}
\alias{delta_hoi_permutation_model}
\title{Permutation Model for Differential Higher-Order Interactions}
\usage{
delta_hoi_permutation_model(
  X1occ,
  X2occ,
  n = 5000,
  conf = 0.05,
  triangle_list = NULL,
  values = FALSE
)
}
\arguments{
\item{X1occ}{Binary occurrence matrix for condition 1}

\item{X2occ}{Binary occurrence matrix for condition 2}

\item{n}{Number of group permutations (default: 5000)}

\item{conf}{Confidence level for the lower/upper bounds (default: 0.05, 
equivalent to 5th and 95th percentile)}

\item{triangle_list}{Optional matrix of network triplets on which to test HOI.
If NULL, all possible triplets are tested.}

\item{values}{Logical, whether to return full null distribution values (default: FALSE)}
}
\value{
List containing:
  \item{Stats}{Data frame with Mean, Median, STD, Min, Max, low_thr, up_thr}
  \item{Values}{Matrix of null distribution values (only if values = TRUE)}
}
\description{
Generates null distribution using sample label permutation for HOI analysis.
}
\details{
This model tests whether differential HOI patterns exceed chance expectations
under the null hypothesis that condition labels are arbitrary.
}
\examples{
set.seed(123)
X1 <- matrix(rbinom(30, 1, 0.7), nrow = 6, ncol = 5)
X2 <- matrix(rbinom(35, 1, 0.5), nrow = 7, ncol = 5)

# Test all triplets (use smaller n for example speed)
perm_model <- delta_hoi_permutation_model(X1, X2, n = 100)

}
